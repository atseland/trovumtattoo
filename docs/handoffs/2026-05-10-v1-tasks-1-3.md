# Handoff 2026-05-10 V1 Tasks 1-3

## Status

Oppgave 1-3 fra `docs/tasks/pre-live-finalization-2026-05-10.md` er implementert lokalt.

Ikke behandle dette som endelig v1-closeout: PWA push, SEO/GEO, portfolio carousel og public copy approval gjenstår fortsatt i pre-live-listen.

## Endringer

1. Booking archive for actual bookings:
   - `bookings` har `archivedAt`, `archivedBy` og `archiveReason`.
   - Admin kan arkivere kun etter `completed` eller `cancelled`.
   - Arkiverte bookinger filtreres bort fra aktive prosjekt-, kommende-, dashboard- og notification-flater.
   - Prosjektdetalj har aktiv/arkiv-filter, restore og permanent delete fra arkiv.

2. Customer-scoped new mail:
   - Ny e-post starter fra kundedetalj, ikke global mailflate.
   - Convex-actionen tar `clientId`, henter eksisterende kunde, bruker kundens e-post som eneste mottaker og lagrer outbound mail som `mailThreads` + `mailMessages` etter SMTP-suksess.
   - SMTP-feil kastes videre og gir ikke falsk suksess eller lagret tråd.

3. Instagram contact target:
   - `/kontakt` Instagram-action bruker `https://www.instagram.com/m/trovumtattoo/`.
   - Homepage "Se mer på Instagram" og footer `@trovumtattoo` er beholdt som profil-lenker med vilje.

## Verification

Kjørt:

- `pnpm test:run src/lib/admin/bookingArchivePolicy.test.ts src/lib/admin/customerMailPolicy.test.ts` passerte etter rød test først.
- `npx convex codegen` passerte og regenererte `convex/_generated/api.d.ts`.
- `pnpm typecheck` passerte.
- `pnpm test:run` passerte.
- `pnpm lint` passerte med eksisterende Convex generated-file warnings.
- `pnpm build` passerte.
- `pnpm playwright test tests/e2e/home.spec.ts` passerte.
- `pnpm playwright test tests/e2e/admin.spec.ts` skip'et rent fordi Clerk/Convex e2e-preconditions manglet.

Gjenstår før deploy:

- Kjør authenticated admin e2e i miljø med `NEXT_PUBLIC_CONVEX_URL` og `E2E_CLERK_USER_EMAIL`.
- Kjør SMTP smoke for customer-scoped new mail i miljø med mail-config.
- Kjør `just verify` rett før eventuell push/deploy.
